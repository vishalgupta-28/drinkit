import { ComingSoon } from "@/components/shared/ComingSoon";
export default function Page() {
  return (
    <ComingSoon
      title="Cart"
      emoji="🛒"
      note="The cart lives in the bottom sheet (CartSheet). This route can host a full-page checkout."
    />
  );
}
